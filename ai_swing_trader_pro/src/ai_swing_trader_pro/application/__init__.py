"""Application layer: use cases / orchestration logic.

Intentionally empty in Sprint 2. Use cases (e.g. `PlaceOrderUseCase`) will
depend only on domain interfaces, never directly on SQLAlchemy or Kite
Connect, per Clean Architecture's dependency rule.
"""
